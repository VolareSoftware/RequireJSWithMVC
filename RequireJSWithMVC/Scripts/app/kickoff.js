define([],
    function () {
        "use strict";

        var init = function () {
            // Common app code run on every page can go here
        };

        return {
            init: init
        };
    }
);